<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="HTB4.Views.CaseDrainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:expandable="clr-namespace:Expandable;assembly=ExpandableView"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    xmlns:vm="clr-namespace:HTB4.ViewModels"
    Title="Case Drain Calculator"
    ios:Page.UseSafeArea="true">

    <!--<ContentPage.BindingContext>
        <vm:CaseDrainViewModel />
    </ContentPage.BindingContext>-->

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="Primary">#2196F3</Color>
            <Color x:Key="Accent">#96d1ff</Color>
            <Color x:Key="LightTextColor">#999999</Color>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <ScrollView VerticalOptions="FillAndExpand">
            <StackLayout
                Padding="24"
                Spacing="0"
                VerticalOptions="FillAndExpand">
                <StackLayout VerticalOptions="CenterAndExpand">
                    <Label
                        x:Name="WelcomeLabel"
                        FontFamily="{StaticResource font_medium}"
                        FontSize="16"
                        Text="WELCOME TO"
                        TextColor="{StaticResource dark_yellow}" />
                    <Label
                        x:Name="PosyLabel"
                        FontFamily="{StaticResource font_heavy}"
                        FontSize="48"
                        Text="POSY"
                        TextColor="#ffffff" />

                </StackLayout>
                <!--<TableView Intent="Form"
                       HasUnevenRows="true">
                    <TableSection Title="Case Drain Calculator">
                        <EntryCell Keyboard="Numeric"
                                   x:Name="rpmCell"
                                   Label="RPM"
                                   Text="{Binding Rpm}" />
                        <EntryCell Keyboard="Numeric"
                                   x:Name="ccRevCell"
                                   Label="CC/rev"
                                   Text="{Binding CCRev}" />
                        <EntryCell Keyboard="Numeric"
                                   x:Name="Efficiency"
                                   Label="Efficiency"
                                   Text="{Binding Efficency}" />
                        <ViewCell>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*5" />
                                    <ColumnDefinition Width="*5" />
                                </Grid.ColumnDefinitions>
                                <Button Text="Calculate"
                                        Grid.Column="0"
                                        BackgroundColor="#EE3F60"
                                        TextColor="White"
                                        Command="{ Binding CalculateCaseDrainCommand }"
                                        VerticalOptions="Center"
                                        x:Name="calculateCaseDrain"
                                        HorizontalOptions="Fill" />
                                <Button Text="Clear"
                                        Grid.Column="1"
                                        BackgroundColor="#4CC7F2"
                                        TextColor="White"
                                        Command="{Binding ClearCommand}"
                                        x:Name="clearCaseDrainButton"
                                        VerticalOptions="Center"
                                        HorizontalOptions="Fill" />
                            </Grid>
                        </ViewCell>
                    </TableSection>
                    <TableSection Title="Output">
                        <TextCell TextColor="Green"
                                  Text="CC/min"
                                  Detail="{Binding CCMinOut}" />
                        <TextCell TextColor="Green"
                                  Text="L/min"
                                  Detail="{Binding LMinOut}" />
                        <TextCell TextColor="Green"
                                  Text="GPM"
                                  Detail="{Binding GpmOut}" />
                    </TableSection>
                    </TableView>

                    <TableView Intent="Form"
                               HasUnevenRows="true">
                        <TableSection Title="GPM needed for Cyl Cycle Time">
                            <EntryCell Keyboard="Numeric"
                                       x:Name="areaCell"
                                       Label="Area"
                                       Text="{Binding Area}" />
                            <EntryCell Keyboard="Numeric"
                                       x:Name="distanceCell"
                                       Label="Distance"
                                       Text="{Binding Distance}" />
                            <EntryCell Keyboard="Numeric"
                                       x:Name="secondCell"
                                       Label="Seconds"
                                       Text="{Binding Second}" />

                            <ViewCell>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*5" />
                                        <ColumnDefinition Width="*5" />
                                    </Grid.ColumnDefinitions>
                                    <Button Text="Calculate"
                                            Grid.Column="0"
                                            BackgroundColor="#EE3F60"
                                            TextColor="White"
                                            Command="{ Binding CalculateCycleTimeCommand }"
                                            VerticalOptions="Center"
                                            x:Name="calculateCycleTimeButton"
                                            HorizontalOptions="Fill" />
                                    <Button Text="Clear"
                                            Grid.Column="1"
                                            BackgroundColor="#4CC7F2"
                                            TextColor="White"
                                            Command="{Binding ClearCommand}"
                                            x:Name="clearCycleTimeButton"
                                            VerticalOptions="Center"
                                            HorizontalOptions="Fill" />
                                </Grid>
                            </ViewCell>
                        </TableSection>
                        <TableSection>

                            <TextCell TextColor="Green"
                                      Text="GPM"
                                      Detail="=" />
                        </TableSection>
                    </TableView>-->
            </StackLayout>
        </ScrollView>

    </ContentPage.Content>
</ContentPage>